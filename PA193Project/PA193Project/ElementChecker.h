#pragma once
#include <string>
#include <iostream>
using namespace std;


/*
* Function to verify if element is a valid HTML5 element.
*
* return true   if element is valid
*        false  if element is not valid
*/
bool isValidElement(string element);
